# Grok Native TUI × Pi Feature Matrix


**Minimum Pi version: 0.84.3** (system `pi` / `@earendil-works/pi-coding-agent`). `pi-main` is an optional git submodule, not required at runtime.

Status definitions: **Native** = implemented by a Grok Pager component; **Adapted** = Pi semantics converted and projected into a Grok native component; **Boundary** = Pi RPC does not expose it or it is bound to a Grok product backend, deliberately not implemented.

## Terminal and Display

| Feature | Status | Implementation |
|---|---|---|
| Terminal init/restore | Native | Grok `init_terminal` / `restore_terminal` |
| Fullscreen / alternate screen | Native | Grok screen mode; selected at startup |
| Minimal / scrollback-native | Native | `xai-grok-pager-minimal`; selected at startup |
| Welcome / minimal logo | Native+Adapted | Defaults to Welcome (consistent with stock `grok`); `ExternalUiProfile.logo` injects π block art (line-width pad prevents centering drift); only `grok-pi -c/--continue` skips Welcome and goes straight to Resume |
| Welcome menu (Pi) | Native+Adapted | Resume/Ctrl+S ≡ `/resume` (Pi catalog); hides New worktree; Changelog opens `https://github.com/Dwsy/grok-pi/blob/main/CHANGELOG.MD` |
| Welcome session prewarm (Pi) | Adapted | Entering Welcome starts `new_session` in the background; first keystroke attaches the prewarmed agent, avoiding cold-start "Starting session…" |
| grok-pi product tutorial (`/tutorial`) | Native+Adapted | Reuses upstream `TutorialState`, `ModalWindow`, picker, Markdown/doc viewer, aliases (`/tour`, `/onboarding`) and keyboard/mouse routing. The grok-pi composition installs an 18-topic `TutorialProfile` covering the native terminal/input surface; Pi multi-provider models, thinking, tools, context, sessions and tree; review/rollback/Plan; extensions, Remote TUI, Skills, Prompt Templates, Packages, themes and resource trust; background tasks, subagents/dashboard, optional automation, export/update/state isolation and diagnostics. Each topic labels default-on, F2 opt-in, restart, experimental and boundary behavior. Stock Grok keeps its default copy; minimal mode remains gated. |
| Update check/install | Adapted | Official source order is GitHub Releases → official scoped npm metadata (`@dwsy/grok-pi`) → JSP proxy; installation still uses official install.sh/ps1. `grok-pi update` / `--check` / Welcome **Ctrl+U**; `GROK_PI_NO_AUTO_UPDATE=1` disables background checks |
| Agent Dashboard | Native+Adapted | Native `/dashboard` · Ctrl+\\ · list/peek/dispatch; Pi keeps one live local AgentView per single-session RPC host, blocks a second dispatch while a turn is busy, and projects completed sessions via `pi/session/list` → `pi/ui/session_catalog` into the dormant roster; not wired to Grok leader FleetView |
| Prompt editing | Native | PromptWidget |
| Settings panel (F2) | Native (Grok-compatible) | **F2**, `/settings`, and the command palette all open the canonical `views/settings_modal` surface. grok-pi uses the same Grok modal chrome, tabs, section sidebar, search, picker/editor behavior, shortcuts, reset confirmation, and responsive layout; its external registry keeps Pi-specific rows and host-declared features visible alongside the shared settings. Pi actions such as `pi_config`, Pi built-in tools, Pi feature toggles, and Pi model slots are handled by the native settings flow. |
| Multiline / Vim mode | Native | Grok slash/settings |
| Theme / timestamps / mouse | Native+Adapted | Grok appearance/input; Pi theme JSON mapped to Grok `Theme` via `theme::pi`, `/theme` accepts `pi:<name>`; built-in experimental `pi:transparent` (dark) and `pi:transparent-light` themes leave the main canvas to the terminal default background (for terminal opacity/blur) while retaining opaque selection, code, diff, and tool surfaces; F2 controls OSC 9;4 terminal-tab progress, off by default |
| Voice dictation | Native+Adapted | Pager-native `/voice` / Ctrl+Space/F8 writes xAI STT text into the PromptWidget; grok-pi explicitly enables this narrow Pager-owned surface. It uses the local Grok login/API-key credential and does not affect Pi model, session, or prompt ownership. |
| Markdown / code blocks | Native+Adapted | Pi text/reasoning → ACP chunks → `xai-grok-markdown` |
| Tool cards | Native+Adapted | Pi tool events → ACP `ToolCall`; `read`/`bash`/`edit`/`write`/`grep`/`find`/`ls` projected to native cards |
| Todo / plan list | Native+Adapted | F2 `[ui].pi_todo` **default on** (restart required) injects grok-pi's built-in `todo` tool; `details.tasks` → ACP `Plan` → native TodoPane/badge and the raw `todo` card is suppressed. While native Todo is on, resource policy blocks `npm:@juicesharp/rpiv-todo`; turn `pi_todo` off to use that compatible provider instead. |
| Plan mode | Native+Adapted | Pi exposes only Normal ↔ Plan: `/plan-mode` and Ctrl+Shift+T toggle, while `/plan` enters; Shift+Tab remains thinking-level cycling. Pager-native Plan toggle → adapter-owned `Inactive/Pending/Active/ExitPending` tracker; full/sparse system-reminder prefix; session-private `.plan.md` sidecar; injected Pi `tool_call` gate blocks `edit`/`write`/`bash` except the plan file; Pi `exit_plan_mode` opens native `x.ai/exit_plan_mode` approval and persists `.plan-mode.json` state |
| Goal mode (`/goal`) | Adapted (MVP legacy) | F2 `[ui].pi_goal` **default off** (restart required). Injected extension: `/goal` + `update_goal` + control file; adapter GoalHost emits native `GoalUpdated` (`goal_detail` / status). Active → `agent_settled` follow-up continuation. **Not** full shell multi-agent classifier/planner/strategist (residual). |
| Loop scheduler (`/loop`) | Adapted (MVP) | F2 `[ui].pi_loop` **default off** (restart required). Injected extension: `/loop` + `scheduler_create/delete/list` + process timers; adapter maps bridge → native `ScheduledTask*` (tasks pane). Session-only (no durable / loop subagent). |
| Diff rendering | Native+Adapted | edit-like metadata enters the native Grok tool/diff pipeline. grok-pi exposes an external-only F2 opt-in, **Side-by-side edit diffs** (default off): when enabled and wide enough, expanded EditTool and the ordinary fullscreen viewer show old/new columns with `-`/`+` markers; disabled or narrow layouts use the native unified renderer, and code-review keeps its unified dual-gutter layout |
| Images | Native+Adapted | Pi image blocks → ACP `ImageContent`; actual terminal display depends on Grok/terminal capability |
| Scroll / find / copy / transcript / export | Native | Grok Pager |

## Agent and Streaming Semantics

| Pi feature | Status | Mapping |
|---|---|---|
| Prompt | Adapted | ACP prompt → Pi `prompt` |
| Mid-turn send now | Adapted | Composer send-now → locally held `Steering` lane row (same isolated queue as follow-ups), forwarded to Pi as `steer` at the assistant `message_end` safe-point flush — identical observable delivery timing, but the row stays removable/editable until then; rows still held at settle dispatch as the next turn's prompt. A pending row can also be sent immediately via `x.ai/queue/interject`; edit-then-send uses the row version. Rows discovered only in Pi's external queue remain read-only because stock RPC cannot remove them atomically. |
| Follow-up queue | Adapted | Active-turn client prompts and extension `sendUserMessage(..., { deliverAs: "followUp" })` calls stay in an adapter-owned isolated queue until dispatch; extension steers (`deliverAs: "steer"`) are held in the same isolated queue's steering lane instead of interjecting immediately. Promoted rows re-enter Pi with RPC source; `queue_update` messages that bypass interception are mirrored in a separate external lane. |
| Abort | Adapted+Boundary | ACP cancel synchronously clears adapter-owned client/extension rows and completes their waiters, drops extension continuations emitted while cancellation is active, then fire-and-forget notifies Pi `abort` (`abort_bash` for Bash). A `get_state` settle probe reopens dispatch only after Pi is idle. Stock Pi external rows remain an RPC boundary because 0.81.1 exposes no `clear_queue`. |
| Text stream | Adapted | `message_update` → AgentMessageChunk |
| Thinking/reasoning stream | Adapted | `message_update` → AgentThoughtChunk |
| Tool start/update/end | Adapted | ACP `ToolCall`/`ToolCallUpdate` |
| Pi Bash background task / Send to Background | Native+Adapted | `grok-pi` private Bash extension holds foreground and initial background Bash subprocesses; foreground reuses Pi `createBashToolDefinition` output/rendering semantics. Pager native Send to Background transfers control via `x.ai/terminal/background` using a controlled temp file keyed by `toolCallId` to the **same** subprocess, then projects to the existing `x.ai/task_*` card; native task-card kill uses `x.ai/task/kill` over the same control channel (`op:kill` + published `runningTaskIds`); `is_background` + `description`, `get_task_output` / `wait_tasks` / `kill_task` remain usable. Foreground Bash auto-promotes after the shared configurable max-wait threshold (default 4.5 min), and each blocking task wait is capped to the same threshold so a still-running task yields a fresh agent turn instead of holding the prompt cache past TTL. `0`/negative disables both behaviors. Terminal state is published out of band on the private `__pi_grok_bash_task__` status channel so it survives streaming, ESC, and a cleared follow-up queue; the conversation bridge message still wakes the model. The adapter mirrors task lifetime, projects each terminal state exactly once across both channels, and settles tasks orphaned by a Pi child exit as `signal: session_restart` (row leaves the running filter, no new failure block). |
| Pi subagents | Native+Adapted | F2 `[ui].pi_subagents` is default on and restart-required. V1 keeps the built-in Pi child `AgentSession`, native `SubagentBlock`/Tasks Pane/child `AgentView` projection, product-isolated `.grok-pi/agents/*.md` + `~/.grok-pi/agents/*.md` definitions, history/wait/cancel, and parent→child follow-up/steer. Optional V2 (F2 → Agent → "Pi subagents V2", or `PI_GROK_SUBAGENTS_V2=1`) adds Codex-inspired `/root/...` paths that are stable within the current root Pi session, plus `spawn_team_agent`, queue-only `team_send_message`, triggering `team_followup_task`, `team_wait`, `team_list`, `team_interrupt`, nested spawn, and automatic `FINAL_ANSWER` return to the parent. `spawn_team` loads project `.grok-pi/teams/*.json` > global `~/.grok-pi/teams/*.json` > bundled `research`/`implementation`/`review` presets. V2 semantic messages use `pi-grok-team-message/v2`; all UI-only `pi-grok-subagent/v1` lifecycle and child updates use one process-private ordered socket, whose readiness is required before child startup. Low-frequency recovery snapshots live in `<parent-session>.subagents.jsonl`; `session/load` explicitly replays them after the root barrier and waits for a request-scoped socket `replay_complete` marker, and neither bridge nor snapshot entries are appended to the parent Pi JSONL. Completed agents become `IDLE`; reactivation preserves the underlying Pi child session but rotates the Pager child/run identity and stores leaf boundaries so each run restores only its own transcript. Background concurrency is capped at 4 with cancellation-safe queueing and atomic preset startup. Product guide: `docs/usage/subagents-v2.md`. Model-driven end-to-end acceptance remains pending. |
| Workflow (Rhai / `/workflow`) | Upstream engine + Pi spawn seam | **Session host + slash surface:** reuses `xai-workflow` + `ExternalWorkflowRuntime`; adapter `x.ai/workflow/{launch,pause,stop}` + `x.ai/workflows/list` + `workflow_updated`; injects `/workflow`, `/workflows`, `/create-workflow` (+ named scripts); hides `__pi_workflow_*` bridge cmds; Pager local handlers + F2 gate. `/create-workflow` is Pager PassThrough user prompt (not a Pi skill). Project scripts default to `<repo>/.grok-pi/workflows`. Live deep-research handtest still recommended. |
| Prompt completion | Adapted | Pi `agent_settled` remains the normal completion barrier. Adapter-promoted client rows keep their ACP waiter; extension/Pi-owned runs emit `x.ai/session/prompt_complete`. An idle probe closes prompts consumed by input handlers without `agent_start`/`agent_settled`, preventing ghost “Waiting…” state. |
| Retry | Adapted | Grok native sticky status/toast |
| RPC connection resilience | Adapted | Unexpected Pi RPC child exit triggers automatic respawn with backoff (storm guard: max 3 recoveries per 5 min), fresh bootstrap, and `switch_session` reattach of the pre-crash session file; deliberate teardowns (probes, respawn) carry `intentional` and are skipped. A `get_state` heartbeat watchdog (15 s cadence; 4 consecutive deadline misses ≈ 2 min unresponsive) kills a live-but-wedged child into the same recovery path (`PI_GROK_RPC_WATCHDOG=0` disables). In-flight prompts/queue rows are cancelled, not replayed. |
| Compaction | Native+Adapted | `/compact [instructions]` → Pi `compact`; Pi `compaction_*` → native CompactionStarted/Completed/Failed/Cancelled scrollback blocks + sticky status. Successful full summaries are projected live and on replay as a native foldable Markdown `SessionEventBlock`, collapsed by default. |
| Session recap (`/recap` + auto away) | Adapted | initialize `meta.sessionRecap`; `x.ai/recap` → inject extension `__pi_grok_recap` (called on `complete` side, appends a non-context custom entry via `appendEntry`, so summaries never enter the agent loop context; legacy sendMessage-era custom messages are stripped from LLM context by the extension's `context` hook) → custom entry `pi-grok-recap/v1` (`entry_appended`) → `SessionRecap`. Uses only the `recap_model` explicitly configured via F2, never falls back to the current session model; auto: ≥3 turns, last completed turn ≥3 minutes old, generated in background while terminal unfocused, not repeated if no new turn after success; manual: any user turn qualifies; optional `/recap [focus]` / `/summarize [focus]` injects `customInstructions` into the recap prompt (append, same pattern as `/compact`); input limited to last 6 turns/12k chars; body language prefers macOS `AppleLanguages`, then …
| BTW history (`/btw` + `/btw-history`) | Adapted | Live deltas/answers stream through `appendEntry("pi-grok-btw/v1", …)` custom entries — never `sendMessage`, so bridge traffic never enters the agent loop context (legacy sendMessage-era custom messages are stripped from LLM context by the extension's `context` hook). Successful answers are also written with `appendEntry("pi-grok-btw/history/v1", …)` as non-context custom entities containing the question, answer, timestamp, request id, and used model. Adapter rebuilds the active branch from `get_entries` on load/tree switch and projects records onto native `BtwBlock` scrollback; the dynamically advertised `/btw-history` command refreshes and displays the same records without invoking a model. |
| Queue pane / count | Adapted+Boundary | Adapter-owned client/extension rows — including mid-turn steer rows — support real remove, clear, edit, reorder, and interject operations before dispatch, so a queued steer is cancellable until its safe-point forward. Stable ids and versions preserve Pager reconciliation and original display text. Pi `queue_update` still feeds a read-only external lane for messages that bypass interception; only follow-up dequeue advances `runningPromptId`, while steering remains in the current turn. Queue drain mode remains settable via `pi/queue/mode` (`one-at-a-time` / `all`). |
| Context bar used tokens | Adapted | Pi `contextUsage` / message usage → ACP `_meta.totalTokens` → top-right bar |
| Context click / `/context` | Native+Adapted | Grok `x.ai/session/info` → Pi stats + messages + `__pi_context_breakdown` + optional `cacheMetrics` (from `get_entries`, pi-cache-graph formula) → native `ModalWindow` (`ContextInfoBlock` + views `0/1/2/3/s`, `e` export, `r` refresh); F2 `[ui].pi_cache_graph` default on; live refresh while running, not written to scrollback |
| User prompt markdown (grok-pi) | Native | F2 `[ui].pi_user_markdown` default on; collapsed/truncated prompts keep the native 3-line `UserPromptBlock` preview, while expanding renders the body with agent Markdown and preserves user chrome. Toggling the setting keeps the current fold state; off uses the classic plain-text renderer throughout. |

## Model, Session, and Commands

| Feature | Status | Notes |
|---|---|---|
| Provider/model catalog | Native+Adapted | Pi owns the multi-provider registry, credentials, `models.json` local/custom endpoints and extension `registerProvider`; `get_available_models` → Grok native model selector, bare `/model` opens it with the active model pinned to top. The default-on Pi auth bridge provides `/login`/`/logout` through Remote TUI when enabled. |
| Thinking effort | Adapted | Pi levels → Grok effort selector; xhigh/max normalized for capability |
| New session | Adapted | Grok `/new` → Pi `new_session` |
| Rename | Adapted | Grok `/rename` → Pi `set_session_name` |
| Resume session catalog | Adapted | `/resume` reads Pi JSONL metadata through the headless adapter. Named sessions receive a native `named` badge; expanded Pi rows show CWD/session path, start/update time, model, message count, persisted token total and cost when recorded. Catalog remains ordered by latest activity. |
| Session info / context snapshot | Adapted | Native `/session-info` (alias `/session`, Pi name) → Grok `x.ai/session/info` ← latest Pi `get_session_stats`. Pi-style scrollback now includes session name/file/ID, total/user/assistant/tool-call/tool-result counts, prompt/cache/output/total tokens, cache hit rate/write volume, and total cost; Grok runtime/auth/model/current-context details remain below. Injected breakdown still supplies system/tool-defs/AGENTS for `/context`, with zero fallback on bridge failure. |
| Session history replay | Adapted | Retained `get_entries` append-log cache with `since` deltas; active `leafId` is resolved by linear parent-chain push+reverse (matching recent Pi upstream), sibling branches are excluded, and tree switching skips full state/model/command plus nested `get_tree` reloads. Replays messages before compaction plus visible summary/custom entries; persisted compaction summaries keep their own type and reappear as foldable summary blocks instead of assistant text. Falls back to compacted `get_messages` on older hosts. |
| Continue previous session at startup | Adapted | `grok-pi --continue` / `-c` → Pi `--continue` |
| Startup resources, prompts, and session options | Adapted | First-class Pi flags forwarded by `grok-pi`: model (`--provider`/`--model`/`--models`/`--thinking`), session (`--session`/`--session-id`/`--session-dir`/`--fork`/`--no-session`/`--name`), prompts (`--system-prompt`/`--append-system-prompt`), resources (`--extension`/`--no-extensions`/`--no-skills`/`--no-context-files`), tools (`--tools`/`--exclude-tools`/`--no-tools`/`--no-builtin-tools`), trust/network (`--approve`/`--no-approve`/`--offline`); remaining args after `--` still passthrough. `--resume` not exposed (Welcome/`/resume`) |
| Pi extension/prompt/skill commands | Native+Adapted | `get_commands` → Grok slash registry; `source=extension` reaches the Pi command handler directly via private ACP metadata, not the Pager-local or Pi steering/follow-up queue; prompt/skill keep prompt semantics |
| Pi Config resource management | Native+Rust compatible | F2 or `/pi-config` (alias `/pi-resources`) → Pi resources; Rust reads Pi `settings.json`/`trust.json`, managing extensions/skills/prompts/themes across global and trusted-project overrides. Discovers resources by Pi's auto-expansion entry rules; source tree collapsed by default, GitHub/npm/local identities clearly visible, search expands only matching sources. Native two-pane supports tree expand/collapse, search, keyboard paging/scroll, click and wheel; right pane previews package.json key fields and README; after switching prompts restart or Pi `/reload`; does not include `install/remove/update` |
| Pi model management center | Native+Rust compatible | `/pi-models` (aliases `/model-config`, `/models-config`) opens a responsive Provider → Model → Details modal modeled on PSM's information hierarchy. Keyboard and mouse support search, add/clone/edit/delete, optional-boolean cycling, dirty state, confirmation, empty/error states and current-model activation. Rust preserves unknown `models.json` fields and uses a directory lock, external-edit conflict check, raw-byte backup, private same-directory atomic replacement and latest-backup restore. Save/restore reuses Pi's official `/reload` (`ctx.reload()`), refreshing the live model catalog without restarting; availability still follows Pi auth (`apiKey`, `/login`, or CLI), and activation uses typed ACP `session/set_model`. |
| Grok cloud/session history picker | Boundary | depends on Grok session store; Pi profile does not expose `/history` |
| Pi session tree (`/tree`) | Adapted | Native `SessionTree` modal: filter/search/collapse/detail/copy/tags; Enter/`Shift+Enter` calls `ctx.navigateTree` (can summarize) via injected extension; `session/load` replays; TreeX-style detail panel; does not modify Pi source |
| Session code review (`/review-session`, `/review-message`) | Native | PSM code-review mechanism → native Pager modal: left file/path list (flat or tree, without per-row diffstat), right BlockViewer edit preview, and a lower Q&A panel with streamed answers plus a separated bottom input (default filter = changes only). **Opt/Alt+Shift+R** opens the session review; F2 `review_file_tree` default **off** (persisted); modal `t` toggles tree (cwd-relative, compact Java packages with `.`). Tree mode folds: `h` collapse / `l` expand / `Enter` toggle dir, `▸/▾` glyphs; mouse click on a dir header toggles its fold; `/` filter auto-expands collapsed dirs. Preview hunk navigation `]`/`[` jumps between diff hunks; file switching is `.`/`,` (both panes); `n`/`N` reserved for viewer search next/prev; bare `d`/`u` half-page; `?` opens a keymap overlay (any key/click closes). `/review-message` reuses jump ListOverlay. No Pi custom UI. |
| Pi session fork (`/fork`) | Adapted | External profile: jump-style prompt `ListOverlay` from RPC `get_fork_messages`; select → RPC `fork` creates branched session file, same agent rebinds to new `sessionId`, `session/load` replays, selected text prefills prompt; Grok peer-agent `/fork` unchanged for non-external |
| Pi session clone (`/clone`) | Adapted | External profile: RPC `clone` duplicates current leaf into a new session file; same agent rebinds to new `sessionId`, `session/load` replays, prompt cleared (Pi parity) |
| Pi resource reload (`/reload`) | Adapted | External: `__pi_reload` → `ctx.reload()`; blocks on streaming **and** compacting (Pi parity), then resets Pager-projected extension UI exactly as Pi resets its extension UI; adapter refreshes command/model catalogs; Pager rescan Pi themes (`rediscover`), including edits/removals, and re-applies active `pi:*` theme; loading/success toast copy aligns with Pi interactive; no session-file branch |
| Pi HTML export / share | Adapted | Grok `/export` stays Markdown transcript; default-on `/export-html` (Pi HTML, or `.jsonl` path) and `/pi-share` (private gh gist with canonical `session.html` + pi.dev viewer) via `pi-grok-export` injection; no second TUI |

## Extension UI

| Method | Status | Grok component |
|---|---|---|
| Bundled extension UI registration | Native+Adapted | Bundled extensions own `extensions/<name>/grok-pi.json`; build-time discovery imports boolean F2 settings plus Cmd+P placement (`section`/`order`/optional label/shortcut) into the generic host manifest. Pager still renders native SettingsRegistry/palette surfaces, and command-palette rows appear only when Pi reports the matching extension command. |
| `notify` | Native+Adapted | warning/error → native toast; explicit `info` → `SystemMessage` scrollback only (Pi `showStatus` / chat append, never toast-only); `/notify` uses a native searchable modal to view all in-process, Pi-session-isolated info/warning/error events (not persisted) |
| `setStatus` | Native+Adapted | sticky banner/status |
| `setWidget` | Native+Adapted | persistent native banner surface |
| `setTitle` | Native+Adapted | terminal title |
| `set_editor_text` | Native+Adapted | PromptWidget |
| `select` | Native+Adapted | QuestionView option list |
| `confirm` | Native+Adapted | QuestionView Yes/No |
| `input` | Native+Adapted | QuestionView freeform PromptWidget |
| `editor` | Native+Adapted | QuestionView multiline PromptWidget |
| timeout/cancel | Adapted | Pi timeout revokes the corresponding QuestionView, returning `cancelled:true` |
| Native Q&A (`ask_user_question` tool) | Adapted | F2 `[ui].pi_ask_user_question` **default off** (restart). Injected extension registers tool; adapter opens multi-question `x.ai/ask_user_question` → native QuestionView; control-dir returns answers to tool result. F2 `[ui].pi_ask_user_question_notifications` **default on** immediately controls native desktop notifications when Q&A arrives while grok-pi is unfocused. Tagline: *Grok Build asks the right questions to nail the details.* Conflict packages: `assets/native_feature_conflicts.toml` — when on, host blocks listed npm packages (e.g. `@juicesharp/rpiv-ask-user-question`); F2 description lists them. |
| raw terminal hook | Boundary | Pi RPC explicitly does not support it |
| custom header/footer/component | Boundary | Pi RPC explicitly does not support component factory |
| Remote TUI (experimental) | Experimental | `PI_GROK_REMOTE_TUI` default-on: **does not modify Pi source**; npm/Node Pi starts through its official `rpc-entry.js`, so argv-only third-party RPC guards do not see outer `--mode rpc`; the first injected compatibility extension projects `ExtensionRunner`'s extension-visible `ctx.mode` from `rpc` to `tui` only while the Remote TUI host is active. Pi core and JSONL transport remain real RPC. It injects the `ctx.ui.custom` host + `setWidget` frame projection; keys via temp keyfile; Pager ANSI parsing. Default-on bare `/login`/`/logout` via `pi-grok-auth` (resume-x style); broader `/pi-*` selectors remain opt-in (`PI_GROK_NATIVE_COMMANDS`) |
| Native feature package conflicts | Host policy | Defaults: `assets/native_feature_conflicts.toml`. Runtime overlays (no rebuild): `$GROK_HOME/native-feature-conflicts.toml` then `$GROK_PROJECT_DIR/native-feature-conflicts.toml` (package union). Gated by F2/bridge: `pi_ask_user_question`, `pi_goal`, `pi_workflows`, `pi_subagents`, and `pi_btw`; disabling a feature admits its conflicting packages again. User `allow` still wins. |
| `rpiv-btw` | Boundary | blocked when F2 `pi_btw` on; use native `/btw` + adapter `x.ai/btw` + `pi-grok-btw` extension (opt-in, default off) |

## Slash Commands

### Retained Grok Native Commands

`exit`, `help`, `hotkeys` (aliases `shortcuts`/`keys`), `tutorial` (aliases `tour`/`onboarding`), `new`, `compact`, `model`, `effort`, `rename`, `resume`, `session-info` (alias `session`), `tree`, `tree-map`, `fork`, `clone`, `reload`, `notify`, `dashboard`, `recap`, `btw`, `copy`, `find`, `jump`, `review-session`, `review-message`, `transcript`, `export`, `expand`, `queue`, `multiline`, `compact-mode`, `vim-mode`, `theme`, `timestamps`, `timeline`, `toggle-mouse-reporting`, `voice`, `doctor`, `debug`, `pi-config`, `pi-models`, `pi-shortcut-manager`. Individual visibility/capability gates still apply. When F2 `pi_btw` is enabled, the Pi extension also advertises `/btw-history` as a direct, model-free history view.

### Dynamic Pi Commands

Extension, Prompt Template, and Skill commands returned by Pi are not hard-coded in Rust. They enter the Grok native slash suggestion/dropdown through the ACP command catalog; name conflicts are de-duplicated by the Grok registry. Built-in bridge extensions can also add Pi-provider auth (`/login`, `/logout`), Pi HTML export/share (`/export-html`, `/pi-share`) and F2-gated workflow/automation commands.

### Deliberately Excluded

Stock Grok product or local session-store commands—including Grok `/history`, Grok account `/login`/`/logout`, `usage`, `plugins`, `mcp`, `memory`, `workspace`, and `share`—are excluded. The same bare `/login`/`/logout` names may be supplied by grok-pi's Pi auth extension, where they authenticate Pi model providers rather than Grok.com. The original `/minimal` and `/fullscreen` re-exec commands are also not exposed: switch screen mode at startup so Pi process arguments are preserved.
